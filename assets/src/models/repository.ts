export type Repository = {
  node_id: string,
  full_name: string,
  activated: boolean,
  private: boolean,
  html_url: string,
  url: string,
  events_url: string,
  pulls_url: string,
  ssh_url: string,
}
